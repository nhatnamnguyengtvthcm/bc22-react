// Khai báo các action types

const PREFIX = "todo";

export const CHANGE_FILTER = `${PREFIX}/CHANGE_FILTER`; // todo/CHANGE_FILTER
export const CHANGE_SEARCH = `${PREFIX}/CHANGE_SEARCH`;
export const GET_TODOS = `${PREFIX}/GET_TODOS`;
